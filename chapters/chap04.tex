\chapter{Results}
\label{chap:results}

% section intro
In this chapter, we show the experimental results of the above discussed algorithms.
We compare them with a baseline called \algRand{}, where the principal chooses which subsets to reveal uniformly at random from the as of yet unrevealed subsets.

% experimenty jsou i z clankuu
We first show results utilizing the game-theoretic motivation, with the utopian gap (\Cref{def:gap}) as the divergence.
These results are already published by \cite{uradnik2024reducing}, along with a very well-performing heuristic for uniform supermodular games.
In this thesis, we extend these results for different divergence functions, utilizing the $ L_k $-norms.
We compare these results to those with the utopian gap.
Finally, we show some examples documenting properties of the divergence already discussed above.
% TODO: workshop?
% Some of the results concerning norms were already submitted to the OptLearnMAS workshop \filip{citace? chceme toto?}\david{asi ne, workshop nikoho nezajímá :D}.

% Metacentrum technical info
The code for conducting the experiments was written in Python~3.10 using \texttt{stable\_baselines3}~2.0 \citep{stable-baselines3}, \texttt{gymnasium}~0.28 \citep{towers_gymnasium_2023} and \texttt{pytorch}~2.0 \citep{Ansel_PyTorch_2_Faster_2024}, and is available at Github \citep{gitrepo}.
We ran all experiments on a cluster with AMD~EPYC~7532~CPUs running at 2.4~GHz.
When running the experiments, we utilized 15~cores and 12~GB of RAM.

\section{Experimental Domains}

In this section, we explore different distributions of set functions/cooperative games and see how our methods compare for different choices of the budget $ \tau $ and the divergence function $ \ell $.
As we have shown in \Cref{thm:algfo-time-complexity-double-exp}, the time complexity of the optimal variants of the algorithms is double-exponential for sufficienty large $ \tau $.
For this reason, we only present the results for $ n=5 $, for which the optimal algorithms took approximately 60 hours to compute.
Using the complexity bound from \Cref{thm:algfo-time-complexity-double-exp}, we estimate that for $ n=6 $, to compute the optimal algorithms for all choices of $ \tau $, it would require over 100 years with our current setup.
% TODO: \david{možná teda přidáš něco o greedy. V tom případě to sem připiš}

There are countless options to choose from for the divergence function $ \ell $.
In this thesis, we focus on the performance of our algorithms with the utopian gap as divergence, because it is well motivated in cooperative game theory.
We further show the performance with the $ L_1 $ and $ L_2 $ norms as divergence functions.
These are very general, not tied to a specific application, and they have a geometric interpretation which ties them to the original motivation of the divergence -- to measure the size of the set of extensions.
See \Cref{sec:divergence} for more details.
As we will see, the results are very similar for the two $ L_k $ norms, and we hypothesize that this will be the case even for other choices of $ k $.

\subsection{Factory Game}

% factory game class
The first setting where we run our experiments is a class of cooperative games we call the \emph{factory games}.

\begin{defi}[Factory game]
  Let $ N = \left\{ 1, \ldots, n \right\} $ be a set of players and $ o \in N $ be an \emph{owner}.
  Then a \emph{factory game} has a characteristic function $ v_o $ defined as \[
    \fce{v_o}{S} \deq \begin{cases}
      \absolute{S} - 1 & \text{if $ o \in S $,} \\
      0                & \text{otherwise.}
    \end{cases}
  \]
\end{defi}

We imagine that this is a very simple model of a factory, with $ n-1 $ equally productive workers, and one owner of the factory.
If the owner is present in the coalition, the workers can work in the factory, each producing a value of $ 1 $.
Otherwise, the workers cannot work, and thus produce nothing.

The point of this example is not to precisely model a real-world situation.
It is rather to have a finite class of games with very simple structure, which are superadditive, and on which we can thus run our algorithms and see how they perform.
% TODO: tohle
\filip{tady bych chtěl říct něco jako že tím že je to malá třída tak jsme schopný identifikovat dobře co se děje, ale zatím nevím jak}\david{Něco jako že dokážeme enumerovat celou tu třídu a tak přesně spočítat ty expectations?}

% factory distribution
For a given $ n \in \N $, there are $ n $ different factory games, each with a different owner.
When running the experiments, we choose $ \fdist $ to be a uniform distribution on these $ n $ games, which we call $ \factory $.
\Cref{fig:factory} shows the results of our experiments for $ n=5 $ as a function of the budget $ \tau $, for different choices of divergence functions.

\begin{figure*}[t!]
  \centering
	\includegraphics[width=\stdfigwidth]{figures/factory5.pdf}
	\includegraphics[width=\stdfigwidth]{figures/l1_factory5.pdf}
	\caption{ The divergence as a function of budget $ \tau $ on the $ \factory[5] $ distribution for the utopian gap \citep{uradnik2024reducing} and the $ L_1 $-divergence. }
	\label{fig:factory}
\end{figure*}


% explain the observed
We can see that, no matter the divergence function, all our proposed algorithms outperform the \algRand{} approach, where each coalition is chosen uniformly at random.
Further, both the offline algorithms are significantly outmatched by the oracle variants.
As expected, the PPO algorithm starts off close to the offline algorithm.
As budget grows, and it gains information about the specific game (in this case, information about which player is the owner), it noticeably outperforms the offline algorithms, and approaches the oracle algorithms.

% comparison of divergence functions
\filip{tady by se dalo zmínit že je to graph game, kdybychom to pak definovali někde}
\david{souhlas, to řekni. ML bude mít radost :D}
Now, let us compare the utopian gap and the $ L_1 $-divergence.
% TODO: finish rewrite
\filip{čekám na experimenty, tady chybí odstavec :) ale jinak ten dalsí je zmasirovanej}

% greedy aint optimal
Let us now focus on the differences between the performance of the greedy and optimal algorithms.
We have not yet discussed whether the greedy algorithms behave optimally, or if there is a gap between the greedy and optimal algorithms.
This is best illustrated in \Cref{fig: fixed owner factory}, where we change the factory distribution to always yield the game with player 1 as the factory owner.
This distribution has support size of 1, and thus the offline algorithms become identical to the oracle algorithms.
While the greedy and optimal algorithms are close to each other, around step $ \tau = 5 $, the optimal algorithms are in fact strictly better than their greedy counterparts.

\subsection{Supermodular}
\label{sec:supermodular}

% remind what supermodular is
The class of supermodular functions is widely studied in many fields, most notably in economics and cooperative game theory.\filip{citace}
It has a very rich structure, and many interesting properties \filip{citace?}\david{Mistr určitě něco bude znát}.
Our experiments were conducted on a uniform distribution of monotone supermodular games on $ n $ players, where the ground coalition has unit value.
We call this distribution $ \supermodular $.
There is an efficient algorithm for sampling from such a distribution \filip{citace}.
The results of those experiments for $ n=5 $ are presented in \Cref{fig:supermodular}.

% explain the observed
Surprisingly, our algorithms work remarkably well on the $ \supermodular[5] $ distribution for all our three choices of the divergence function.
All algorithms vastly outperform the \algRand{} approach, achieving over 99~\% of gap reduction with just the budget of $ \tau = 5 $.
Further, our algorithms exhibit very similar performance, suggesting that knowledge of the specific function drawn from the distribution is of little help when decreasing the gap.
To illustrate the strategy used by each algorithm, \Cref{fig:supermod_coals} shows the distribution of revealed subsets for $ \tau = 1, \dots, 12 $\david{co tady dát $\tau\in\{1,\dots 12\}$? ale je to jen random thought jako většina toho co píšu :D}\filip{jasný. jakožeee nevím, tak jak to mám já mi přijde že to signaluje že to prochází tau od 1 do 12... ale nemám silný názor. co Mistr na to?}.
We investigate these findings further in \Cref{ssec:largest}.

\section{Offline vs Oracle}

% TODO: offline beats oracle?
% \david{Možná spíš offline vs oracle?}
% \filip{Uuuuh, faaajn, ale offline beats oracle mi přišlo chytlavější teda xD}
% \david{Jakože zní to cool, ale ten title pak naznačuje, že je to takhle vždycky}

% re-introduce what it's about
In \Cref{chap:pp}, we have proven various statements about the relationship between the solutions of the \algFO{} and \algRO{} algorithms, along with the relationships of the optimal algorithms and their greedy counterparts.
The last relationship we have not mentioned yet is that of \algFG{} and \algRG{}.
One would expect that the resulting valuation of \algFG{} would be an upper bound on \algRG{}, as was the case with the optimal variants.
This has been the case in all the figures we have seen so far.
However, as we show in \Cref{fig:offlinebeatsoracle}, it is \emph{not} true in general.

% TODO: explain l-inf
Our example uses the $ L_\infty $ norm as $ \ell $, which is defined as \[
  \fce {L_\infty} x \deq \max_i x_i.
\]

% TODO: explain why

\filip{schůzka: nevím kolik a na co tam chceme dát ty obrázky přes celou stránku}
\david{Jn, ať to nepůsobí jako spam. Ale minimálně nějaký se tam hodí mi přijde. Mohlo by se na ně odkázat v té sekci o Largest Coalitions.}

\section{Largest Coalition Heuristic}
\label{ssec:largest}

% explain motivation
The main motivation of this thesis is to find out which subsets/coalitions are in some sense the most important for a given distribution of set functions.
To do this for any given distribution of superadditive set functions, we have devised the algorithms described in \Cref{chap:pp}.
For some distributions, these algorithms are more effective, while for others they are nearly comparable to revealing the subsets at random.\david{Co takhle sem přidat něco jakože když jsme lepší než random, náš přístup navíc řekne jaký subsety jsou ty doležitý a další instavec by mohl říct něco jakože to ilustrujeme na supermodular? Ale klidně řekni, že se ti tahle sekce líbí a necháme jí :D}

In this section, we further investigate the $ \supermodular $ distribution, and we hypothesize about the generalization of what we have seen in our experiments for larger $ n $.
We have previously seen (\Cref{sec:supermodular}) that for $ n=5 $, all algorithms perform well, while behaving all in the same way -- first choosing the $ n $ largest subsets\david{tohle mi furt přijde moc silný, co takhle initially behaving in the same way?}\filip{přidal jsem tam že to platí pro $ n=5 $, což je prostě pravda}\david{No, záleží ještě PPO je "our algorithm". Same mi ilustruje, že policy všech těch algoritmů jsou identický. To není pravda žejo, proto bych místo same dal very similar a nikdo nemůže říct ani popel}.
After those are revealed, the divergence decreases by about 99 \% (depending on the choice of $ \ell $).

% comment on performance
This suggests that the \textquote{most important subsets} for the supermodular functions, at least in terms of the divergence, are in fact the largest subsets, in addition to the minimal information $ \k_0 $.
We thus propose the heuristic we call the \algLC{}, which, for a budget of $ \tau = 2n + 2 $, reveals the subsets $ \k = \k_0 \cup \left\{ N \setminus \left\{ i \right\} \suchthat i \in N \right\} $.
Unfortunately, we cannot compare this heuristic to the optimal algorithms for $ n > 5 $,  due to their time complexity (\Cref{thm:algfo-time-complexity-poly-tau}).
We hence compare it only with the \algRand{} strategy.
\Cref{fig:largest} shows the performance of this heuristic as a function of $ n $, with the utopian gap and the $ L_1 $-divergence.
In order to make the results comparable, we normalize the results by the initial divergence (gap), i. e. when $ \k = \k_0 $.

% explain why
Surprisingly, while the normalized divergence increases for the \algRand{} strategy as $ n $ grows, for our \algLC{} heuristic it stays the same, or even decreases.
In other words, while a random subset structure of size $ n $ carries \textquote{less information} as $n$ grows, the largest subsets get \textquote{more important} (again, in terms of the divergence).
We can thus reduce the gap to near-zero, while keeping the number of revealed subsets \emph{linear} in $ n $, instead of the exponentially many of them we would require to know the entire function.

To understand why that is the case, we have analyzed the average values of subsets in the supermodular distribution.
We have found that the values decrease by several orders with the size of the subset.
This explains the good performance of our heuristic, as choosing large subsets limits the upper superadditive bounds of the smaller subsets the most.
