\chapter{Preliminaries}

\section{Set functions}

% co je set funkce
Set functions are very general objects studied extensively in countless fields, including
matroid theory, game theory utility functions and countless others. % TODO: citace a konkrétní příklady


% definice set funkce
\begin{defi}[Set function]
	Let $ N $ be a finite set.
	A \emph{set function} on the set $ N $ is any function $ f: \pot N \to \R $.
\end{defi}

Though the definition of a set function consideres a general finite set $ N $, in this thesis,
we will for simplicity only consider set functions on the set $ N = \{1, 2, \ldots, n\} $.
This simplification is common in the literature,  % TODO: citace; jinak, proste jsou izomorfni
as it allows us to be more concrete when discussing set functions, while
the results we show can be easily generalized to the case of a general finite set $ N $.

% k čemu jsou
For example, $ N $ can represent a set of \emph{goods}, while the set function $ f $ can represet a \emph{utility} an agent receives from obtaining a subset of the goods.

% monotonicita
When studying set functions modelling real-world problems, we can often assume that the set function has certain properties.
One of the most natural and commonly observed of such properties is \emph{monotonicity}.

\begin{defi}[Monotonicity]
	A set function $ f: \pot N \to \R $ is \emph{monotonically non-decreasing} if \[
		\left( \forall S \subseteq T \subseteq N \right)\quad \fce{f}{S} \leq \fce{f}{T},
	\]
	and \emph{monotonically non-increasing} if \[
		\left( \forall S \subseteq T \subseteq N \right)\quad \fce{f}{S} \geq \fce{f}{T}.
	\]
\end{defi}

% ukázat na utilitě
In our example of a set function representing utility, assuming the utility of each good by itself is non-negative, the monotonicity property is equivalent to the assumption that acquiring more goods can never decrease the utility of an agent.
% TODO: ne vždycky

% superadditivita
Another very common and natural property of many set functions is \emph{superadditivity}.

% TODO: jak to udělat s tím že máme i subadditivitu? -- nejdriv super, pak note: sub
\begin{defi}[Superadditivity]
	A set function $ f: \pot N \to \R $ is \emph{superadditive} if \[
		\left( \forall S, T \subseteq N; S \cap T = \emptyset \right)\quad \fce{f}{S \cup T} \geq \fce{f}{S} + \fce{f}{T}.
	\]
\end{defi}

% ukázat na utilitě
Getting back to our example with utility, superadditivity means that the utility of obtaining two sets of goods together is at least as high as the sum of the utilities of obtaining each of the sets separately.
In other words, a set is at least as valuable as the sum of its parts.

% supermodularita
\begin{defi}[Supermodularity]
	A set function $ f: \pot N \to \R $ is \emph{supermodular} if \[
		\left( \forall S, T \subseteq N \right)\quad \fce{f}{S \cup T} + \fce{f}{S \cap T} \geq \fce{f}{S} + \fce{f}{T}.
	\]
\end{defi}
% TODO: proč je supermodularita sexy
% TODO: hrozně sexy struktura

% submodularita
\begin{defi}[Submodularity]
	A set function $ f: \pot N \to \R $ is \emph{submodular} if \[
		\left( \forall S, T \subseteq N \right)\quad \fce{f}{S \cup T} + \fce{f}{S \cap T} \leq \fce{f}{S} + \fce{f}{T}.
	\]
\end{defi}
% TODO: vztah se supermodularitou
% TODO: proč submodularita je taky sexy
% TODO: greedy algoritmy a matroidy
% TODO: třídy funkcí

% TODO: capitalization?
\section{Cooperative game theory}

% co je to kooperativní teorie her
Cooperative game theory is a field of mathematics that studies situations in which agents can form cooperating groups, called \emph{coalitions}.
These coalitions receive a \emph{payoff} from the game, which is then somehow divided among the agents in the coalition.
The goal of cooperative game theory is to study the \emph{stability} of the formed coalitions, and the \emph{distribution} of the payoff among the agents.

% definice kooperativní hry
\begin{defi}[Cooperative game]
	A \emph{cooperative game} is a pair $ \left( N,v \right) $, where $ N $ is a finite set of \emph{players} and $ v: \pot N \to \R $ is the \emph{characteristic function} of the game.
	Further, we require that $ \fce{v}{\emptyset} = 0 $.
\end{defi}

When discussing coalitions, we usually use the terms \emph{grand coalition} for the coalition of all players $ N $, and \emph{singletons} for coalitions containing a single player.

% TODO: vztah se set functs
From the definition it is clear that the characteristic function is a set function on the set $ N $, with the additional requirement that the value of the empty coalition is zero.

% supermodularita => superadditivita
% TODO: kopirovat do setfunkcí
\begin{prop}[ ]
	Let $ \left( N,v \right) $ be a cooperative game.
	If the characteristic function $ v $ is supermodular, then it is superadditive.
\end{prop}

% TODO: nebudeme chtít tohle mít pro všechny set funkce? chceme to řešit v preliminaries nebo radši až potom v definici lower/upper?
\begin{proof}
	Suppose $ S,T \subseteq N $, such that $ S \cap T = \emptyset $.
	We will use the definition of supermodularity, along with the fact that $ \fce{v}{\emptyset} = 0 $.
	We get \[
		\fce{v}{S} + \fce{v}{T} \leq \fce{v}{S \cup T} + \fce{v}{S \cap T} = \fce{v}{S \cup T} + \fce{v}{\emptyset} = \fce{v}{S \cup T},
	\]
	which is exactly the definition of superadditivity.
\end{proof}

% TODO: classes of games

% payoff vector
In cooperative game theory, we usually assume that the grand coalition has formed, i.e. all the players are cooperating, then we discuss how the value of the grand coalition, $ \fce{v}{N} $, shall be split between the players, and then investigate whether the formed coalition is stable, or not.
The payoff of players is described by a payoff vector.

\begin{defi}[Payoff vector]
	Let $ \left( N,v \right) $ be a cooperative game.
	A \emph{payoff vector} is $ x \in \R^n $.
	The payoff vector is \emph{efficient} if \[
		\sum_{i = 1}^{n} x_i = \fce{v}{N}.
	\]
\end{defi}

For simplicity, we usually shorten the notation of sum over the values of the payoff vector.
For a general coalition $ S \subseteq N $, we denote \[
	\fce{x}{S} \deq \sum_{i \in S}^{} x_i. % TODO: tohle lip: pouzivame rozsireni setfunkci bla bla; also, dat vic na zacatek?
\]
% efficiency
In this thesis, we will only consider efficient payoff vectors, as we want to distribute the entire value of the grand coalitions.
Other notions of distribution where the payoff vector needn't be efficient is sometimes studied, but we will not discuss them here. % TODO: citace

% solution concept
We are interested in studying payoff vectors with certain properties, such as fairness, stability, etc.
For this, we define solution concepts as the sets of payoff vectors with given properties.
Formally

\begin{defi}[Solution concept]
	A \emph{solution concept} is a function $ f: \games n \to \pot{\R^n} $.
\end{defi}

% TODO: masirovat
Some solution concepts, so-called \emph{multi-point solution concepts}, give sets of payoff vectors of varying size, depending on the game.
However, some important solution concepts are so-called \emph{single-point}, that is, they map each game to exactly one vector.
In those cases, we simplify the notation to leave out the set, yielding a solution concept with the signature $ f: \games n \to \R^n $.

The first important solution concept we will mention here is the \emph{core}.
The core the payoff vectors, which encourage stability of the grand coalition.
In other words, when distributing $ \fce{v}{N} $ according to a payoff vector from the core, the players don't have incentive to leave the grand coalition.

\begin{defi}[Core]
	The \emph{core} is a multi-point solution concept defined as \[
		\core v \deq \left\{ x \in \R^n \suchthat \fce{x}{N} =\fce{v}{N} \land \left( \forall S \subseteq N \right)\! \left( \fce{x}{S} \geq \fce{v}{S} \right) \right\}.
	\]
\end{defi}

If the core of a game is empty, it tells us that no matter how we distribute the value of $ \fce{v}{N} $, there will always be a set of players that would be better off leaving the grand coalition, so it is unstable, and thus (assuming perfect rationality of the players), it won't form.

% shapley
However, the core doesn't consider the fairness of the distribution.
For that, we would like another solution concept, that would divide the value of the grand coalition proportionally to the contributions of each player.
By contributions, we mean the extra value the coalition generates, when a player joins. Formally, for $ i \in N $ and $ S \subseteq N \setminus \left\{ i \right\} $, the contribution of $ i $ to $ S $ is $ \fce{v}{S \cup \left\{ i \right\}} - \fce{v}{S} $.

\begin{defi}[Shapley value]
	The \emph{Shapley value} is a single-point solution concept defined as \[
		\shapley v_i \deq \sum_{S \subseteq N \setminus \left\{ i \right\}}^{} \frac{s! \left( n - s - 1 \right)!}{n!} \left( \fce{v}{S \cup \left\{ i \right\}} - \fce{v}{S} \right),
	\]
	where $ s \deq \absolute{S} $.
\end{defi}

So, is the Shapley value fair?
That obviously depends on your definition of fairness, which can be very subjective.
However, the Shapley value is generally accepted as fair, as it satisfies many natural qualities that a reasonable fair solution concept should satisfy.

% TODO: citace
\begin{prop}[Characterization of the Shapley value]
	The Shapley value $ \Shapley $ is the unique function satisfying the following properties: \begin{enumerate}[ ]
		\item efficience, $ \sum_{i \in N}^{} \fce{f_i}{v} = \fce{v}{N} $,
		\item symmetry, \[
				\left( \forall i,j \in N \right)\! \left( \left( \forall S \subseteq N \setminus \left\{ i,j \right\} \right)\! \left( \fce{v}{S \cup i} = \fce{v}{S \cup j} \right) \implies \fce{f_i}{v} = \fce{f_j}{v} \right),
			\]
		\item null player, $ \left( \forall i \in N \right)\! \left( \left( \forall S \subseteq N \setminus i \right)\! \left( \fce{v}{S} = \fce{v}{S \cup i} \right) \implies \fce{f_i}{v} = 0 \right)   $,
		\item additivity, $ \left( \forall v,w \in \games n \right)\! \left( \fce{f}{v + w} = \fce{f}{v} + \fce{f}{w} \right) $.
	\end{enumerate}
\end{prop}
% TODO: další vlastnosti SV?

\section{Online Learning}

% co to je
Machine learning is a field of computer science, which tries to approximate solutions we are unable to solve using traditional algorithms.
For example, when asked to write an algorithm that can recognize objects in images, one wouldn't even know where to start.
% TODO: example
\todo[inline]{Tohle je divná definice machine learning, i když skvělou taky nemám. Líbí se mi definice co řekne google. Ale každopádně ten příklad je divnej - že nevíš kde začít ještě neznamená, že to nejde vyřešit `tradične' (whatever it means). Taky bych vypíchnul, že tyhle pojmy jsou dost obecné a my je představíme tak, jak je chceme i použít.}

In online learning, the agent learns gradually, by interacting with the environment, and receiving feedback on its actions.
Online learning is a very general model, which can be used to model many different problems, including multi-agent systems.
In this thesis, we will focus on the online learning of a single agent.
\todo[inline]{Za mě online learning je podmnožina machine learning, kde data dostáváš v diskétních časech a děláš nějaké akce. Pro nás relevantní je, že pozorovaná data můžou záviset na akcích co jsi dělal v minulosti.}


% TODO: muze byt obecne, my si vybirame strategii

% agent+environment
Now, let us define the model a bit more\todo{lol :)} formally.
% TODO: používám tady "game", což by chtělo někde nahoře zmínit co je
We will start with a model, where, though the game is played repeatedly, one run of the game only consists of a single move (in words of game theory, the game \emph{isn't} sequential\todo{simultaneous-move game}).
We will extend this model, so one run of the game will consist of multiple moves, later when discussing regret and reiforcement learning.
\todo[inline]{Z tohohle odstavce jsem zmatenej. Moc nevim co se tu říká a proč je to relevantní. Vždyť my vůbec neřešíme nějakou hru více hráčů, prostě bych to všechno bral jako interakci s black-boxem.}

% TODO: daviduv oblibeny obrazek
\begin{defi}[Online learning]\label{def:online-learning}
	The agent has a set of \emph{actions} $ A = \left\{ 1, \dots, a \right\} $.
	% TODO: popsat "dist" značení
	He chooses a distribution $ \pi \in \dist A $\todo{Psal bych $\Delta^{|A|}$}, which specifies with which probability he will play each action.
	He then receives a \emph{loss} $ \ell \in \R^a $, which specifies the loss of the agent for each action.
\end{defi}
\todo[inline]{Tahle definici mi přijde nic neříká. Není tam nic o tom, že se to děje v krocích třeba. A jsem celkem smutnej z lossů - v RL se bude mluvit o rewardech, tak proč to hned nesjednotit?}

Of course, when applying this model in the real world, the agent must choose one specific action, and receive the corresponding loss.
However, we are interested in what happens \emph{on average} over all the actions chosen according to $ \pi $.
This motivates the following definition.

\begin{defi}[Experienced loss]
	Suppose the agent chose a distribution $ \pi \in \dist A $ and received $ \ell \in \R^a $.
	Then the \emph{experienced loss} is $ \exl \deq \sum_{i=1}^a \ell_i \pi_i $.
\end{defi}

Note that when using the model, we will run this model repeatedly, for $ T \in \N $ steps, and the agent's action distribution $ \pi $ will change to reflect the losses received in earlier runs.
When discussing losses from the $ t $-th run, we will denote them $ \ell^t $ ($ \exl^t $ etc.).
Further, let us formalize the loss received by the agent up to the $ t $-th run as follows.

\begin{defi}[Cumulative loss]
	Suppose the agent received losses $ \ell^1, \dots, \ell^t $.
	Then the \emph{cumulative loss} is $ \cl^T \deq \sum_{t=1}^{T} \ell^t $.
	Further, the \emph{cumulative experienced loss} is $ \cxl^T \deq \sum_{t=1}^{T} \exl^t $.
\end{defi}
\todo[inline]{Nějak bych ty definice sjednotil a dal od začátku jako repeated interaction. Taky není úplně jasně vidět, co je vektor a co číslo - použil bych bold pro vektory.}

Note also that we haven't specified how exactly the losses at each run are generated.
We can imagine that there is a function $ E: \dist A \to \R^a $, which computes the losses.
However, mind you that this function can change between different runs of the game.
% TODO: příklad (asi ten s deštníkama?)
We can imagine that the function comes from a distribution of functions.
This distribution is unknown to the agent, though gathers information about it from the losses he receives as he plays the game.

\subsection{Regret Minimization}

% cil RM
Regret minimization is a technique used in online learning, where the agent tries to pick a strategy, that would have been good in hindsight.
By hindsight, we mean that the agent compares itself other sequences of strategies he could have chosen, and tries to minimize the difference between the losses he experienced, and the losses the best sequence of strategies would have experienced.

More formally, we say that the agent compares again some \emph{comparison set} $ \algs $ of online algorithms.
An online algorithm can be thought of as a function $ \alg $, which maps a sequence of already received losses to a distribution over the actions.
We won't want to compare against all possible algorithms, because among them, there will always be one that always guesses the best action, which will lead to the lowest possible loss.
% TODO: put this in an example environment?
For an adversarial environment, this is impossible to compare against.
\todo[inline]{Tohle není pravda - porovnat se můžeš, akorát takovýhle regret nejde minimilizovat proti libovolnému black-boxu. Tohle diskuzi bych odložil po definici regretu.}

Instead, we will be very general, and define so-called \emph{regret} for a general comparison set $ \algs $, and later discuss behavior of regret for specific comparison sets.

% regret
\begin{defi}[Regret]
	Let $ \algs $ be a comparison set.
	Let $ \ell^1, \dots, \ell^T $ be a sequence of losses received by the agent, playing a sequence of strategies $ \pi^1, \dots, \pi^T $.
	Then the \emph{regret} of the agent after $ T $ steps, with respect to $ \algs $, is \[
		R^T_{\algs} \deq \cxl^T  - \min_{\alg \in \algs} \cxl^T_{\alg},
	\]
	where $ \cxl^T_{\alg} $ is the cumulative experienced loss of the algorithm $ \alg $ computed from the losses $ \ell^i $ received by the agent.
\end{defi}

% předpokládáme že losses zůstanou stejné
Let us emphesize that experienced losses of the algorithms from $ \algs $ are computed from $ \ell^1, \dots, \ell^T $.
This means, that those aren't the losses the algorithms would have actually experienced, had they been played instead of the agent, as the loss $ \ell^i $ depended on the the distribution $ \pi^i $ chosen by the agent.
Consider the simple adversarial example, where the loss $ \ell_i $ is $ 1 $ for all actions with non-zero probability, and $ 0 $ otherwise. 
Suppose that the agent chooses action $ 1 $ with probability $ 1 $, and an algorithm $ \alg $ chooses action $ 2 $ instead.
Then the experienced loss of the agent is $ 1 $, while the experienced loss of $ \alg $, as seen from the definition of regret, is $ 0 $, even though, had we followed $ \alg $, we would have experienced loss $ 1 $, too.
\todo[inline]{Tomu konci nerozumím. Loss akce 2 je 0 ne? Takže regret k té akci je 1.}

% swap regret, internal, external?
Let us now mention different types of regret, meaning different comparison sets $ \algs $, along with their properties.
The first and probably most studied type of regret is the \emph{external regret}.
Many times in literature, when the term \emph{regret} is used without any further specification, it is meant to be the external regret.\todo{Přesně z tohohle důvodu bych používal instantaneous / cumulative regret. Jen regret je možná moc vágní, kor takhle na začátku v refinicích.}
It is defined on the comparison set $ \algs_A \deq \left\{ \alg_a \suchthat a \in A \right\} $, where $ \alg_a $ is the algorithm that always chooses action $ a $.
The external regret analyzes what would have happened, had the agent chosen a single action, and stuck with it for the entire game.\todo{Tady game není jasné co vůbec je. Možná bych tomu online procesu říkal nějak úplně jinak, ale nenapadá mě dobré jméno.}
The formula can be simplified to \[
		R^T_{\algs_A} = \cxl^T - \min_{a \in A} \cl^T_a.
	\]

For stronger comparison sets, we will use algorithms which are derived from the strategy of the agent, but modify it using \emph{modification rules}.
A modification rule is any function $ F: A \to A $, and the resulting algorithm $ \alg_F $ looks at the strategy $ \pi $ of the agent, and plays $ \pi' $, defined as \[
	\pi'_a \deq \sum_{i: F(i) = a} \pi_i.
\]
Consider the example of a modification rule $ F_{i,j} $, which respects the strategy of the agent, but always plays action $ j $ instead of action $ i $.
Formally: \[
	\fce{F_{i,j}}{k} = \begin{cases}
		j & \text{if } k = i, \\
		k & \text{otherwise.}
	\end{cases}
\]
% TODO: víc popsat?
The regret with respect to the comparison set $ \left\{ \alg_{F_{i,j}} \suchthat i,j \in A \right\} $ is called the \emph{swap regret}.
Even more generally, if we consider a comparison set with all possible modification rules, we get the \emph{internal regret}.\todo{tohle není pravda, internal regret je special case swap regretu.}
% TODO: vlastnosti regretů?

% regret minimizer
Now that we have a way to measure how well the agent is doing, we need to find a way to adapt the strategy of the agent, so that the regret is minimized.
Ideally, we would like to find a strategy, which keeps regret $ 0 $, but as we have already seen with the adversarial example, that is in general impossible.
Instead, we will try to find a strategy, which keeps the regret as small as possible.
More specifically, we are aiming for a strategy, which keeps the regret sublinear in $ T $, that is, $ R^T_{\algs} = \fce{o}{T} $.

\begin{defi}[ ]
	Let $ \algs $ be a comparison set.
	An agent is an \emph{$ \algs $-regret minimizer}, if its $ \algs $-regret satisfies \[
		R^T_\algs = \fce{o}{1}.
	\]
\end{defi}
\todo[inline]{Tady určitě musí být $o(T)$ ne? Možná by bylo fajn ukázat, proč zrovna sublineární regret je to co nás zajímá.}

Once again, when talking about a regret minimizer without any further specification, we mean an external regret minimizer.
External regret is special, because there are algorithms that can provably achieve sublinear regret, and we will discuss them in further sections.
% TODO: vysvětlit že sublin regret je cool

% TODO: příklady regret minimizerů?

% TODO: sequence form game

% TODO: predictive regret?

\subsection{Reinforcement Learning}

% reiforcement learning vs ML
Reinforcement learning is a subfield of machine learning, which studies how an agent can learn to interact with an environment, so as to maximize some notion of cumulative reward.\todo{tohle jde říct přesně ne? Proč some notion?}
It is more natural to define it in a sequential setting right away, so we will do that here.
% TODO: změnit def na sequential
The reinforcement learning setting is similar to the \cref{def:online-learning}, but with the key difference that the agent doesn't submit a \emph{distribution} over the actions to the environment, and receive loss per action.
Instead, he needs to select a single action, and receives a single reward and the new state he's in, as a function of the current state and the chosen action.
\todo[inline]{Tady bych ty rozdíly zformalizoval a až pak se pouštěl do toho, proč jsou třeba neuronky. Můžeš třeba dát jednoduchý příklad nějakého spojitého environmentu, kde by tabulka dělat nešla (pokud nechceš diskretizovat numerickou přesností, což bych tam klidně napsal).}

% drl
There are many approaches to reinforcement learning.
However, in the last few years, the most popular, and empirically most successful approach has been so-called \emph{deep reinforcement learning}.\todo{Tohle nevím jestli není zavádějící. To, že se používá neuronka na approximaci všeho možného neznamená, že je to nějaký nový algoritmus. To je ale co bych si představil pod "approch".}
In deep reinforcement learning, the agent uses a neural network to get the function that maps states to actions.

% TODO: znovu davidův obrázek, tentokrát na RL

% proč RL
One of the reasons why we are interested in reinforcement learning is, that it is able to work with a much bigger state space.
As the state is passed to the neural network, it can for example be a vector of real numbers.
In contrast, recall that in the sequential setting of regret minimization, we needed the set space to be finite (and possibly small), as we needed to have a regret minimizer in each node of the game tree.
Here, we have a single neural network, and the state information is instead used as a part of its input.

% TODO: RL je online i jinak?

% PPO
Deep reinforcement leaning is a very rich and active field.
The reader is encouraged to look at the recent literature to delve deeper into the topic, and see the advances made since the time of writing of this thesis.
In our experiments, we have decided to use the \emph{Proximal Policy Optimization} (PPO) algorithm, which is a state-of-the-art algorithm for reinforcement learning, and has been shown to work well in many applications in all kinds of practical environments.
\todo[inline]{Tady to právě vypadá, že PPO je special case "deep RL". To ale není pravda, můžeš klidně dělat tabular PPO.}

\todo[inline]{Pro naše potřeby by to chtělo asi nějak popsat střeva PPO. Taky bych vypíchnul rozdíly proti regretu - nemá garance, ale zase díky samplování můžešvyřešit i ty spojité problémy třeba. Taky bych ale napsal, že minimalizovat regret a maximalizovat reward je to stejné proti "oblivious environment" (to je to, kde bereš gradient loss podle policy jako 0). Další příklad co mám rád je ale, že fundamentálně je to jiné - regret je typicky happy, když má sublineární regret. Algoritmy jsou typicky dané a mají garance na sublinear regret - ale neminimalizují ho! Simple příklad z Balkovy přednášky - máš si vzít ten deštník, nebo opalovací krém? Když je šance 50/50, tak regret ti vrátí uniformní distribuci a je happy. Ale co když bys měl přístup k předpovědi počasí? Regret je furt happy a neumí to nijak použít, protože ten algoritmus je typicky fixní. Na druhou stranu PPO by s takovym environmentem zatočilo snadno a dosáhlo jen tak btw negarivního external regretu.}