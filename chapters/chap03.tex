\chapter{Tackling the Principal's Problems\filip{29.5}}

\filip{Používám "subset" pro $ S \subseteq N $. Co mám používat pro $ \s \subseteq \pot N $? Zatím používám coalition structure, protože tak se tomu říká v CGT. Ale jak v set funkcích zatím nevím. Subset structure? to by kdyžtak chtělo asi definovat někde}\david{Subset structure se mi líbí, ale možná Mistr bude mít i lepší název. Každopádně souhlas, stojí to rozhodně za definici, někde v preliminaries?}\filip{OK, sounds good. Počkáme ještě co Mistr k tomu}
\filip{Tak jsem přešel na subset system, ale moc se mi to nelíbí... Mistře?}
In this chapter, we first define the different approaches to solving, or approximately solving, the Online and Offline Principal's problems.
We then examine the experimental results of those approaches.
When describing the algorithms, we assume we have a distribution $ \fdist $ (given by the nature of the application) and a divergence function $ \ell $ (which we have chosen).
Later, when discussing the results, we compare the performance of the algorithms on different distributions and divergence functions.

In the following two subsections, we describe the \algFO{}, \algFG{}, \algRO{} and \algRG{} algorithms, which have already been introduced by \cite{uradnik2024reducing}.
In this thesis, we extend the algorithms to a setting with a general divergence function and extend the intuition behind the definition of these algorithms.
Further, we formally describe the time complexity of each algorithm, as it depends on the choice of the size of the budget, and we compare the solutions of these algorithms as they relate to one another, and to the Offline/Online principal's problems.

\section{Solutions to the Offline Problem}

% offline problém řešíme optimálně, jen aproximujeme E
To reach an exact optimal solution to the Offline principal's problem (\david{see }\filip{to nevím jestli chci. jinde to taky nemám. mám to dát všude?}\david{za mě to zní víc jako součást té věty, takže líp. Ale je to ok i bez toho.}\Cref{def:offline-pp}), we would need to compute the expectation $ \fceb{\E_{f \sim \fdist}}{\divg\ell{\k_- \cup \s}} $.
This is sometimes feasible, but only for very small instances, with very specific and artificial distributions and divergence functions.
We use a different approach, replacing the exact expectation with an \emph{approximated} expectation $ \fceb{\EE_{f \sim \fdist}}{\divg \ell {\k_- \cup \s}} $, which we compute by sampling $ \sigma $ samples from the distribution $ \fdist $.
For each sample, we compute the divergence $ \divg \ell {\k_- \cup \s} $ for every possible choice of $ \s $ and average the results.
Then, we choose the $ \s $ which gives the smallest average divergence.
This approach doesn't guarantee the optimal solution, however, with $ \sigma $ large enough, the approximation converges to the optimal solution due to the law of large numbers \cite{Bernoulli1713}.\filip{citace}
\david{Citovat tohle je podle mě správné a je to cool}
% @article{bernoulli1713ars,
%   title={Ars conjectandi, opus posthumum: accedit tractatus de seriebus infinitis, et epistola Gallic{\`e} scripta de ludo pilae reticularis},
%   author={Bernoulli, Jakob},
%   journal={(No Title)},
%   year={1713}
% }
\filip{otázka je co přesně chceme citovat... ty navrhuješ to úplně první objevení, což je asi cool, ale na druhou stranu tady to asi není dokázaný v moderním pojetí psti... ale asi je fakt že my zase nepotřebujeme přesně definovat tohle zrovna...}


% pseudokód \algFO{}
\begin{algor}{\label{alg:fo}\algFO{}}{Function distribution $ \fdist $, number of samples $ \sigma $, the initially known subsets $ \k \supseteq \k_0 $, the remaining budget $ \tau $, a divergence function $ \ell $.}
  {$ \hat{\s_*} $, the (approximate) solution of the offline principal's problem.}
  \State $ D \gets $ a map of zeros of size $ \binom{2^n-\absolute{\k}}{\tau} $, indexed by $ \left\{ \s \subseteq N \setminus \k \suchthat \absolute{\s} = \tau \right\} $.
  \Indent For $ i \in \left\{ 1, 2, \ldots, \sigma \right\} $:
    \State $ f_i \sim \fdist $
    \Indent For $ \s \subseteq \pot{N} \setminus \k: \absolute{\s} = \tau $:
    \State $ D[\s] \gets D[\s] + \divg[f_i] \ell {\k_- \cup \s} $
    \EndIndent
  \EndIndent
  \State $ \hat{\s_*} \gets \argmin_{\s} D[\s] $
\end{algor}
\martin{Nezabere mi krok 3 ještě $2^n$ času, stejně jako Thm 11 (offline greedy time complexity)? Nebo bereš samplování z orákula a nepočítáš tam složitost? Pochopil jsem potom důkaz Thm11 špatně?}
\filip{Dobrej point. Já tu tiše předpokládám, že $ \divt (n) $ je aspoň $ 2^n $... Přidal jsem to tam explicitně jako předpoklad.. už ok?}

% analýza časové složitosti
The time complexity of the \algFO{} algorithm is dependent not only on the size of the problem $ n $, but also on the choice of parameters, namely the number of samples $ \sigma $ and the computation of divergence, which depends on $ \ell $.
We will denote the time complexity of computing the divergence for the specific divergence\martin{dvakrát divergence}\filip{jedno je divergence a druhý divergence function}\david{souhlas s oběma, i když asi víc s Filipem :D Taky se mi zdá, že by to šlo přeformulovat.}\filip{Jako možná by bylo fajn vymyslet lepší název pro $ \ell $ než "divergence function"...} \david{co takhle We will denote the time complexity of computing the divergence given by a specific $l$ as ...}\filip{furt mi přijde jako že by hodně prospělo mít lepší jméno než divergence fn} function $ \ell $ as $ \fce\divt{n} $, a function of $ n $, the size of the ground set.
For simplicity, we will assume that the time complexity of computing the divergence is the same for every possible subset system of size $ \tau $.
This simplifies the notation, and it holds for every divergence function we consider in this thesis.
We further assume that this is $ \fce{\Omega}{2^n} $, which is motivated by the fact that the divergence should take into account all the values of coalitions.
This again holds for every considered divergence function, and it simplifies the resulting time complexity bound.

The last detail we need to address is the actual mechanics of sampling the function $ f_i \sim \fdist $.
We will here assume that the time complexity of this operation is $ \fce{\O}{2^n} $.
We can imagine that these values are pre-generated, and that we just load them into memory, or that we have an oracle that tells us the value for any coalition on demand in constant time.
With this, we can analyze the time complexity of the \algFO{} algorithm.

\filip{je tohle theorem??}\david{spíš ne, lemma?}\filip{lemma mi evokuje jako že by to mělo být jako pomocný tvrzení k nějaký větě... proposition? možná zeptáme se mistra na názor?}
\filip{fyi, všechno dávám zatím jako theoremy, a pak se domluvíme co s tím :) referencuju to všude pomocí cref, takže se to samo změní}
\david{Proposition zní dobře. Taky se mi moc nelíbí to "prove", protože není ještě jasné co dokazuješ. Co třeba formalize?}
\filip{Formalize se mi nelíbí :(}
\david{tak co třeb study the time complexity?}
\filip{změnil jsem to na anaylze, lepší?}
\david{awesome}
\begin{thm}[\algFO{} time complexity]
  \label{thm:algfo-time-complexity}
  Let $ n $ be the size of the ground set, $ \kappa $ the size of the initial information, $ \tau $ the remaining budget, $ \sigma $ the number of samples, and $ \ell $ a divergence function.
  Let $ \fce\divt{n} $ be the time complexity of computing the divergence, $ \fce{\divt}{n} \in \fce{\Omega}{2^n} $.
  Then the time complexity of the \algFO{} algorithm is $ \fce\Theta{\sigma \cdot \binom{2^n-\kappa}{\tau} \cdot \fce\divt{n}} $.
\end{thm}

\begin{proof}
  The algorithm iterates over $ \sigma $ samples, for each sample it computes the divergence for every possible subset system of size $ \tau $.
  The number of possible subset systems of size $ \tau $ is $ \binom{2^n-\kappa}{\tau} $.
  The time complexity of computing the divergence is $ \fce\divt{n} $.
  Therefore, the time complexity of the \algFO{} algorithm is $ \fce\Theta{\sigma \cdot \binom{2^n-\kappa}{\tau} \cdot \fce\divt{n}} $.
\end{proof}

% je to paralelizovatelné
Observe that computing each sample is independent of the others, assuming we have a way to then quickly sum the result.
Thus the computation can be parallelized, which significantly reduces the computation time in practice.
In our Python implementation we do in fact compute every sample in a parallel process, and we use \tprog{numpy} arrays to sum the results, which gives little overhead compared to the overall computation time.

% časové odhady pro různé velikosti instance
For a small, constant budget $ \tau $, the time complexity of the \algFO{} algorithm is exponential in the size of the ground set $ n $.
This is managable for small instances, assuming the divergence function is computable in a reasonable time, and we have enough memory to store the results, $ \fce{\O}{\sigma \binom{2^n - \kappa}{n}}$.
If $ \kappa $ is small, but $ \tau $ grows polynomially with $ n $, the time (and space) complexity becomes worse, as the following \namecref{thm:algfo-time-complexity-poly-tau} shows.

\begin{thm}[ ]
  \label{thm:algfo-time-complexity-poly-tau}
  Suppose the size of the initial information $ \kappa $ is $ \fce{\Theta}{n} $, and the budget $ \tau $ is $ \fce{\Theta}{n^c} $.
  Then the time complexity of the \algFO{} algorithm is $ \sigma \cdot \fce\divt{n} \cdot 2^{\fce{\Theta}{n^{c+1}}}$.
\end{thm}


\begin{proof}
  From \Cref{thm:algfo-time-complexity}, we know that the time complexity of the \algFO{} algorithm is $ \sigma \cdot \binom{2^n-k}{\tau} \cdot \fce\divt{n} $, up to a multiplicative constant.
  It is known that \[
    \left( \frac{n}{k} \right)^k \leq \binom nk \leq n^k.
  \]
  Applying these bounds to $ \binom{2^n-k}\tau $, we get \[
    \left( \frac{2^n-k}{\tau} \right)^\tau \leq \binom{2^n-k}\tau \leq \left( 2^n-k \right)^\tau.
  \]
  Starting with the upper bound, we get \begin{align*}
    \binom{2^n-k}\tau \leq \left( 2^n-k \right)^\tau \leq \left( 2^n \right)^\tau = 2^{\fce{\Theta}{n^{c+1}}}.
  \end{align*}
  Similar analysis can be done for the lower bound, yielding
  \begin{align*}
    \binom{2^n-k}\tau \geq \left( \frac{2^n-k}{\tau} \right)^\tau = \left( \frac{2^{\fce{\Theta}{n}}}{n^c} \right)^{n^c} = 2^{\fce{\Theta}{n^{c+1}}},
  \end{align*}
  where the final equality holds because \[
      \frac{2^{\Theta(n)}}{n^c} = \frac{2^{\Theta(n)}}{2^{\log n^c}} = 2^{\Theta(n) - c \log n} = 2^{\Theta(n)}.
  \]
\end{proof}

\Cref{thm:algfo-time-complexity-poly-tau} shows that for a polynomially growing budget, the time complexity of the \algFO{} algorithm grows super-exponentially in $ n $.
If we wanted to have $ \tau $ even bigger than polynomial in $ n $, the time complexity would grow even faster.
This would happen for example if we wanted to reveal a half of the ground set, meaning $ \tau = \frac{2^n - \kappa}{2} $.
Then, the time complexity of the \algFO{} algorithm would be double exponential.

\begin{thm}[ ]
  \label{thm:algfo-time-complexity-double-exp}
  Suppose the initial information is $ \k_0 $, and $ \tau = 2^{n-1} $.
  Then the time complexity of the \algFO{} algorithm is $ \fce{\Theta}{2^{2^n} \cdot 2^{-\frac{3n}{2}} \cdot \fce\divt{n} \cdot \sigma} $.
\end{thm}

\begin{proof}
  To prove this \namecref{thm:algfo-time-complexity-double-exp}, we will once again use the result of \Cref{thm:algfo-time-complexity}, along with the Stirling approximation of the binomial coefficient \[
    \binom {2m}m \approx \frac{2^{2m}}{\sqrt{\pi m}}.
  \]
  First, let us observe that $ \kappa = \absolute{\k_0} = n + 2 $.
  The wanted complexity is obtained by applying \Cref{thm:algfo-time-complexity} with the following bound \[
    \binom{2^{n}-k}{\frac{2^n - \kappa}{2}} \approx \frac{2^{2^n - k}}{\sqrt{\pi \frac{2^n - k}{2}}} \in \fce{\Theta}{\frac{2^{2^n}}{2^k 2^{\frac n2}}} = \fce{\Theta}{2^{2^n} 2^{-\frac{3n}{2}}}.
  \]
\end{proof}

In our experiments, we want to compare the \algFO{} algorithm for different choices of $ \tau $, so we do reach the double-exponential time complexity.
For an illustration of double-exponential growth, consider computing the \algFO{} algorithm for all choices of $ \tau \in \left\{ 1, \ldots, 2^n-k \right\} $.
Then for $ n=5 $, this process takes $ 30 $ hours on a CPU (details are discussed in the \nameref{sec:results}~\namecref{sec:results}), and for $ n=6 $, we estimate it would take over $ 100 $ years.

% hence, motivace \algFG{} a popis
This problem motivates the following simplification of the \algFO{} algorithm, the \algFG{} algorithm.
Here, we do not consider all possible choices of $ \s $, of which there might be double-exponentially many in $ n $, but rather we build $ \s $ incrementally.
We start with an empty set, and in each step, we reveal the subset which gives the smallest divergence.
This is a greedy approach, and it is not guaranteed to give the optimal solution, but it is much faster in practice.

% pseudokód \algFG{}
\begin{algor}{\label{alg:fg}\algFG{}}{Function distribution $ \fdist $, number of samples $ \sigma $, the initially known subsets $ \k \supseteq \k_0 $, the remaining budget $ \tau $, a divergence function $ \ell $.}
  {$ \hat{\s_*} $, the (approximate) greedy solution of the offline principal's problem.}
  \State $ G \gets $ sample $ \sigma $ samples of $ f \sim \fdist $.
  \State $ \hat{\s_*} \gets \emptyset $
  \Indent For $ t \in \left\{ 1, \dots, \tau \right\} $:
  \State $ S \gets \argmin_{S \in \pot N \setminus \left( \k \cup \hat{\s_*} \right)} \sum_{f \in G} \divg \ell {\k_- \cup \hat{\s_*} \cup \left\{ S \right\}} $
  \State $ \hat{\s_*} \gets \hat{\s_*} \cup \left\{ S \right\} $
  \EndIndent
  
\end{algor}

% analýza časové složitosti OG
\begin{thm}[\algFG{} time complexity]
  Let $ n $ be the size of the ground set, $ \kappa $ the size of the initial information, $ \tau $ the remaining budget, $ \sigma $ the number of samples, and $ \ell $ a divergence function.
  Suppose that the time complexity of computing the divergence is $ \fce\divt{n} $.
  Then the time complexity of the \algFG{} algorithm is $ \fce\Theta{\sigma \cdot \left( 2^n-\kappa \right)\cdot\tau \cdot \fce\divt{n}} $.
\end{thm}

\begin{proof}
  First, we sample $ \sigma $ games (we assume sampling of a single game takes $ \fce\O{2^n} $, i.e. constant time for each subset).
  The algorithm iterates over $ \tau $ timesteps. 
  In each timestep, we compute the argmin, which translates to going over all viable subset systems, of which there are $ 2^n-\kappa $.
  For each subset, we compute the divergence in each sample, reaching $ \sigma \cdot \fce\divt{n} $.
  Putting it all together, we get the required time complexity.
\end{proof}

% TODO: tohle přepsat, potenciálně corollary
We can see that, even for a large $ \tau $ and small $ \kappa $, the \algFG{} algorithm has a worst-case time complexity $ \fce{\Theta}{\sigma \cdot 2^{2n} \cdot \fce{\divt}{n}} $, which is much more managable than $ \fce{\Theta}{\sigma \cdot 2^{2^n} \cdot 2^{-\frac{3n}{2}}\cdot \fce{\divt}{n}} $ of the \algFO{} algorithm.

% paralelizace nejde, leda to trochu upravit....
One thing to point out is that in order to allow greedy choice of $ S $, we had to switch the order of computation -- in the optimal approach, we first compute all the divergences for all samples, and then choose the best subset system, while in \algFG{}, we explicitly don't want to compute all the divergences, which makes parallelization slightly more involved.
We can still parallelize the computation of divergences for each sample, but we have to synchronize the computation of the best subset system in each timestep.
This gives slightly bigger overhead, but it is still considerably faster than the \algFO{} algorithm, due to the far better overall time complexity.

% k čemu to je vlastně?
The offline principal's problem seeks to find a subset system that yields the best approximation of the underlying function, on average.
We study it with the hope that it gives us insight into what subsets are important for a given class of set functions.
In practice, this knowledge would reduce the cost of gathering the remaining values, assuming having some ambiguity is acceptable.
However, if we had a distribution $ \fdist $ based on a real-world situation, to get the greedy solution, we would need multiple samples, for which we know \emph{all} the values of the function, which would be costly.
This can be overcome by either approximating the distribution, and computing the offline solution on the new, approximated distribution, or by studying the real distribution, but on a smaller scale, thus having a managable cost of computing the offline solution.

\section{Approaching the Online Problem Solition}

% wanting online optimal is bad
When solving the \nameref{def:offline-pp}, we perform an exhaustive search over the domain, or the set of all possible subsets.
Though this is double-exponential in the number of players in the worst case, it is at least finite.
In contrast, a solution of the \nameref{def:online-pp} (\Cref{def:online-pp}) is a \emph{policy}, a function that maps the current state to a distribution over the next actions, minimizing the final expected divergence.
The domain of the online problem then is the set of all policies, which is uncounably infinite, and due to that fact, an exhaustive search becomes infeasible.

Instead of finding a solution to the online problem, in this section, we strive to at least bound its optimum.
An upper bound on the divergence of the optimal policy is the divergence of the optimal offline solution, since the offline solition can be seen as a policy where the online information is just ignored.
This is formalised in the following \namecref{thm:online-bound-offline}.

% offline bounds online
\begin{thm}[Offline bounds online]
  \label{thm:online-bound-offline}
  Let $ \fdist $ be a distribution, $ \ell $ a divergence function, $ \tau $ a budget, and $ \k \supseteq \k_0 $ the initial information.
  Further, let $ \s^* $ be the optimal subset system of the corresponding \nameref{def:offline-pp} and $ \pi^* $ be the optimal policy of the corresponding \nameref{def:online-pp}.
  Then \[
    \E_{f \sim \fdist} \val \tau \ell f \k {\pi^*} \leq \E_{f \sim \fdist} \divg \ell {\k_- \cup \s^*},
  \]
  where $ \val \tau \ell f \k {\pi^*} $ is the policy valuation, as defined in \Cref{def:online-pv}.
\end{thm}

\begin{proof}
  We will construct a policy $ \pi^o $, which copies the offline solution $ \s^* $.
  Let us index the coalitions in $ \s^* $ as \[
    \s^* = \left\{ S_1, S_2, \ldots, S_\tau \right\}.
  \]
  Then $ \pi^o $ will be defined as follows: \[
    \fce{\pi^o}{\k, t} = \begin{cases}
      \vec 1_{S_t} & \text{if } t \leq \tau, \\
      \vec 1_{S_\tau} & \text{otherwise,}
    \end{cases}
  \]
  Where $ \vec 1_i $ is a vector with $ 1 $ on the $ i $-th position and $ 0 $ elsewhere.
  Note that for $ t > \tau $, the choice $ \vec 1_{S_\tau} $ is arbitrary, as we never get to it when computing the expectation of $ \val \tau \ell f \k {\pi^o} $, as the theorem requires.

  We can see that the policy $ \pi^o $ is a valid policy, and after $ \tau $ steps, the revealed coalitions are exactly $ \s^* $.
  Thus the valuation $ \val \tau \ell f \k {\pi^o} $ is exactly equal to the divergence of the offline solution $ \s^* $ for every instance $ f \sim \fdist $, so their expectations are also equal.

  Since the optimal solution of the online problem is a minimum over all possible policies, it is bounded by the value of the policy $ \pi^o $, which is the divergence of the offline solution $ \s^* $.
\end{proof}

% idea of oracle optimals
To get a lower bound, let us get back to the motivation for the online problem.
The idea was to spend a portion of the budget to get a more information about the function, and then use this information to make a more informed decision about which values to choose.
Using this intuition, we can clearly see that the best thing the principal can hope for is that the values she reveals tell her \emph{everything} about the function, and thus she can find the exact optimal subset system to minimize the divergence.
To get a lower bound on the online problem, then, we can give the principal an oracle, which tells her all the values in advance, and she can pick the best subset system accordingly.

\begin{defi}[Oracle valuation]
  \label{def:oracle-valuation}
  Let $ \fdist $ be a distribution, $ \ell $ a divergence function, $ \tau \in \N $ a budget, and $ \k \supseteq \k_0 $ the initial information.
  For a given function $ f $, let \begin{equation}\label{eq:def-sf}
    \s_f \deq \argmin_{\s \subseteq \pot N \setminus \k: \absolute{\s} = \tau} \divg \ell {\k_- \cup \s}.
  \end{equation}
  Then the \emph{oracle valuation} of $ \fdist $ is \[
    \oracle \fdist \deq \fceb{\E_{f \sim \fdist}} {\divg \ell {\k_- \cup \s_f}}.
  \]
\end{defi}

% oracle bounds online
The oracle valuation is exactly the lower bound we described above, as the following \namecref{thm:online-bound-oracle} shows.

\begin{thm}[Oracle bounds online]
  \label{thm:online-bound-oracle}
  Let $ \fdist $ be a distribution, $ \ell $ a divergence function, $ \tau $ a budget, and $ \k \supseteq \k_0 $ the initial information.
  Further, let $ \pi^* $ be the optimum of the corresponding \nameref{def:online-pp}.
  Then \[
    \fceb{\E_{f \sim \fdist}} {\val \tau \ell f \k {\pi^*}} \geq \oracle \fdist.
  \]
\end{thm}

\begin{proof}
  Let us fix one $ f \sim \fdist $.
  The value of $ \val \tau \ell f \k {\pi^*} $ is the expected divergence of a trajectory of revealed coalitions according to $ \pi^* $.
  For each trajectory $ S_1, \ldots, S_\tau $, the following holds \[
    \divg \ell {\k_- \cup \left\{ S_1, \ldots, S_\tau \right\}} \geq \divg \ell {\k_- \cup \s_f},
  \]
  from the definition of $ \s_f $ (\david{když píšeš Theorem XY, dal bych i Eq. Z}\filip{eeeh.. já nevim... ale ok}\Cref{eq:def-sf}).
  As such, the same can be said for the expected value over the trajectory, which is exactly the valuation of $ \pi^* $. \[
    \val \tau \ell f \k {\pi^*} \geq \divg \ell {\k_- \cup \s_f}.
  \]
  This holds for every $ f $, so it must also hold in expectation over $ \fdist $, which is what we wanted to prove.
  \filip{Je tohle dost OK co se formalismu týče? (Otázka hlavně na Mistra, promiň Davide :D)}\david{za mě je to až moc fotmální. Celý ten statement je podle mě triviální...}
\end{proof}

% oracle optimal
The advantage of this approach, compared to the online problem, is that now the domain is once again $ \left\{ \s \subseteq \pot{N} \setminus \k: \absolute{\s} = \tau \right\} $, which is finite, and we can perform an exhaustive search over it.
This yields the following algorithm.

% we have the same problem as with the offline optimal -- time, hence greedy
\begin{algor}{\algRO{}}{Function distribution $ \fdist $, number of samples $ \sigma $, the initially known subsets $ \k \supseteq \k_0 $, the remaining budget $ \tau $, a divergence function $ \ell $.}
  {$ \hat O $, the (approximate) value of the oracle valuation (\Cref{def:oracle-valuation}).}
  \State $ \hat O \gets 0 $
  \Indent For $ i \in \left\{ 1, 2, \ldots, \sigma \right\} $:
    \State $ f \sim \fdist $
    \State $ \hat O \gets \hat O + \min \left\{ \divg \ell {\k_- \cup \s}  \suchthat \s \subseteq \pot{N} \setminus \k: \absolute{\s} = \tau \right\} $
  \EndIndent
  \State $ \hat O \gets \frac{\hat O}{\sigma} $
\end{algor}

% comparison to FG
The \algRO{} algorithm is almost identical to the \algFO{} algorithm (\Cref{alg:fo}).
The difference between the two is the order in which we gather and evaluate the actions.
In both algorithms, we sample $ \sigma $ functions, evaluate them on all possible $ \s $, and then, in the \algFO{} algorithm we choose the actions that do the best \emph{on average}, whereas in the \algRO{} algorithm we choose the best action for \emph{each sample}, and only then average the \emph{resulting} divergence.
As such, by the same reasoning as in \Cref{thm:algfo-time-complexity}, we can show that the time complexity of the \algRO{} algorithm is $ \fce\Theta{\sigma \cdot \binom{2^n-k}{\tau} \cdot \fce\divt{n}} $.
That is once again double exponential in the size of the ground set $ n $ for $ \tau $ big enough, and thus infeasible for large instances.

This brings us to the \algRG{} algorithm, which is a greedy approximation of the \algRO{} algorithm.

\begin{algor}{\algRG{}}{Function distribution $ \fdist $, number of samples $ \sigma $, the initially known subsets $ \k \supseteq \k_0 $, the remaining budget $ \tau $, a divergence function $ \ell $.}
  {$ \hat{O} $, the (approximate) value of the oracle valuation (\Cref{def:oracle-valuation}).}
  \State $ \hat{O} \gets 0 $
  \Indent For $ i \in \left\{ 1, 2, \ldots, \sigma \right\} $:
  \State $ f_i \sim \fdist $
  \State $ \s \gets \emptyset $
  \Indent For $ t \in \left\{ 1, \dots, \tau \right\} $:
  \State $ S \gets \argmin_{S \in \pot N \setminus \left( \k \cup \s \right)} \divg[f_i] \ell {\k_- \cup \s \cup \left\{ S \right\}} $
  \State $ \s \gets \s \cup \left\{ S \right\} $
  \EndIndent
  \State $ \hat{O} \gets \hat{O} + \divg[f_i] \ell {\k_- \cup \s} $
  \EndIndent
  \State $ \hat O \gets \frac{\hat O}{\sigma} $
\end{algor}

% RG je jako FG -- čas, prostor, všechno, a tím pádem skvělé
\algRG{} is once again almost identical to \algFG{} (\Cref{alg:fg}), in the same way that the optimal variants were, hence we can use the same reasoning to approximate time complexity.
The resulting value of the oracle valuation is once again clearly an upper bound\david{lower bound je zavádějící, protože lower divergence je lepší}\filip{ajo, má být upper bound ne? jakože celá fráze je upper bound on the valuation of ..., to je ok ne?}\david{když co je valuation? Víš co, já jsem zvyklý uvažovat o rewardech (jsem holt optimista). Pesimisti mají rádi lossy a tak by jim upper bound přišel asi víc fajn. Ale pro mě víc je lepší... :D Takže za mě ok, když řekneš přesně co je valuace. Nebo můžeš celou věc říct nějak nezávisle typu "the greedy algorithm is clearly outperformed by the optimal"}\filip{no ne, "valuation" je prostě to co vrací ty algoritmy. v definici toho algoritmu mám že vrací approximate value of the oracle valuation. To je něco co má svojí definici -- \Cref{def:oracle-valuation}}\david{Ok, hele já nejsem proti téhle formulaci, jen říkám, že mě to zmátlo.} on the valuation of \algRO{}, as here the subset systems considered here for the optimal valuation are a subset of those considered in \algRO{}.

% greedy vs greedy neporovnatelný
Since the Oracle valuation approach was to be an upper bound on the value of the optimal online solution, it begs the question of how the greedy approach compares to the optimal online solution.
The \algRG{} algorithm is in fact incomparable to the online solution.
Empirically, it is in most cases very close or even identical to \algRO{}, but we do have examples where the two differ significantly.
Prehaps more surprisingly, we show that for very special choices of $ \ell $ and $ \fdist $, even \algFG{} yields better results than the \algRG{} algorithm.
For more details, see the \nameref{sec:results}~\namecref{sec:results}.


\subsection{Utilizing PPO}
\label{sec:ppo}

% oracle je teorie, ppo je praktický
We have introduced the oracle approach to find a provable lower bound on the online solution.
However, in practice, the oracle algorithms are infeasible.
This is because they assume we have access to all the values, which defeats the purpose of our approach in the first place.
To get a practical solution of a online problem which is usable\david{tohle mi nedává smysl, spíš něco jako "while being able to use it in practice"?}\filip{na schůzce} in practice, we thus turn to \emph{reinforcement learning}.

% PPO state space
We employ the PPO algorithm (\Cref{alg:ppo}).
For more details about the algorithm, see the \nameref{sec:prel-ppo}~\namecref{sec:prel-ppo} of Preliminaries.
As the state space, we use a vector of size $ 2^n - \kappa $ ($ \kappa $ is the number of initially known subsets), where each element is a value of a subset, or zero if the value is unknown.\david{Tyjo chtěl jsem zrovna říct, že tohle je dobře protože naše values nejsou nula, ale to vůbec není pravda! Tohle bysme měli nějak vymakat ještě...}\filip{jo to mě napadlo už před tím, ale přijde mi že není asi nějakej dobrej způsob jak to tomu PPO sdělit :D možná -1? ale nechal jsem to být s tím že on může furt vybírat dobrý akce pomocí tý masky...}
\david{Zkusim se nad tim ještě zamyslet. Možná by se dalo udělat to, že všechny hodnoty uděláme striktně pozitivní tak, že normalizace všude přičte additivní 1. Ale nevim nevim, zamyslim se}
This precisely captures the state space defined in the \nameref{def:online-pp} (\Cref{def:online-pp}).

% maskable ppo
The action space is the set of all unknown subsets.
Notice that the action space changes depending on the currently known subsets.
We handle this by \emph{masking} the actions that are not available, so the agent cannot choose them.

% state space grows exponentially, což není dobrý
Assuming the size of the initial information $ \kappa $ is sub-exponential, the size of both the state and action space, and thus the size of the input and output of the neural network, is exponential in the size of the ground set $ n $.
For larger $ n $, this makes the network struggle to make sense of all the information, increasing the number of epochs needed for training.
One of the directions for future work is to find an architecture of the network which has the input and output encoded in such a way that its size becomes more managable, and thus the training is more stable as $ n $ increases.

% reward
We have not yet discussed the reward function.
In order to precisely model the online principal's problem, the reward should be the divergence of the revealed subset system, and it should be received after revealing the $ \tau $-th subset.
This means that the agent receives reward all the way at the end, giving him very little information about the performance of a specific action, espetially for large $ \tau $.
In order to make the training more stable, we have decided to alter the reward function.
We give the agent a reward after each action, which is the negative of the divergence of the currently revealed subset system.
This gives the agent more information about the action it has just taken, thus allowing it to learn faster, and in a more stable manner.

% PPO teda není online, ale něco jako online
Consequently, the PPO algorithm does not approximate the online solution exactly.
It is incentivized to reveal the subsets that minimize the divergence \textquote{along the way}, whereas the online problem is interested only in the final divergence.
This might lead the agent to behave in a greedy way.
However, as mentioned above, and as the experimental results show, the greedy approaches are generally very close to the optimum, and the\david{and so PPO...?} PPO works well in practice.

% garance nejsou, proto testujeme empiricky
Unfortunately, while PPO works very well in practice, it does not have any guarantees about the optimality of the solution.
We can only test the algorithm empirically, and compare it to the other approaches, to see how well it performs.

\section{Results}
\label{sec:results}

% section intro
In this section, we show the experimental results of the above discussed algorithms.
We compare them with a baseline called \algRand{}, where the principal chooses which subsets to reveal uniformly at random.

% experimenty jsou i z clankuu
We first show results utilizing the game-theoretic motivation, with the Utopian Gap (\Cref{def:gap}) as the divergence.
These results are already published in \cite{uradnik2024reducing}, along with a very well-performing heuristic for uniform supermodular games.
In this thesis, we extend these results for different divergence functions, utilizing the $ L_k $-norms.
We compare these results to those with the Utopian Gap.
Finally, we show some examples documenting properties of the divergence already discussed above.
% TODO: zmínit workshopovej článek

% Metacentrum technical info
The code for conducting the experiments was written in Python~3.10 using stable\_baselines3~2.0 (\cite{stable-baselines3}), gymnasium~0.28 (\cite{towers_gymnasium_2023}) and pytorch~2.0 (\cite{Ansel_PyTorch_2_Faster_2024}), and is available at \cite{gitrepo}.
We ran all experiments on a cluster with AMD~EPYC~7532~CPUs running at 2.4~GHz.
When running the experiments, we utilized 15~cores and 12~GB of RAM.
\david{možná za něco málo o tom jak dlouho to běhalo?}

\subsection{Factory Game}

\begin{figure*}[t!]
	\includegraphics[width=0.48\textwidth]{figures/factory5.pdf}
	\includegraphics[width=0.48\textwidth]{figures/convex5.pdf}
	\caption{
    The utopian gap as a function of number of revealed coalitions on the {\tt factory}(5) (left), and {\tt supermodular}(5) (right) distributions of games \citep{uradnik2024reducing}.
	}
	\label{fig: exploitability over time}
\end{figure*}

% factory game class
The first setting where we run our experiments is a class of cooperative games we call the \emph{factory games}.
\filip{citace?}
\david{može být a nemusí}

\begin{defi}[Factory game]
  Let $ N = \left\{ 1, \ldots, n \right\} $ be a set of players and $ o \in N $ be an \emph{owner}.
  Then a \emph{factory game} has a characteristic function $ v_o $ defined as \[
    \fce{v_o}{S} \deq \begin{cases}
      \absolute{S} - 1 & \text{if $ o \in S $,} \\
      0                & \text{otherwise.}
    \end{cases}
  \]
\end{defi}

We imagine that this is a very simple model of a factory, with $ n-1 $ equally productive workers, and one owner of the factory.
If the owner is present in the coalition, the workers are allowed to work in the factory, each producing a value of $ 1 $.
Otherwise, the workers cannot work, and thus produce nothing.

The point of this example is not to precisely model a real-world situation.
It is rather to have a simple class of finite games with very simple structure, which are superadditive, and on which we can thus run our algorithms and see what they do.
\david{tohle bych ještě pomasíroval, ale pěkný}

% TODO: factory distribution

% explain the observed
From \Cref{fig: exploitability over time}~(left), we can see that all our proposed algorithms outperform the random approach, where each coaition is chosen uniformly at random from the as of yet unknown coalitions.
Further, there is a noticable difference between the performance of the offline and oracle optimal algorithms.
As expected, the PPO algorithm starts off identical to the offline algorithm, and, as it gains information about the specific game drawn from the distribution, it starts to behave close to the oracle algorithms.

% greedy aint optimal
Another question we can answer from \Cref{fig: exploitability over time} is that of optimality of the greedy approach.
Even though for the factory distribution, the greedy and optimal algorithms are close to each other, around step $ 5 $, the oracle optimal algorithm is in fact strictly better than its greedy counterpart.
The same can be said for the offline algorithms, as, if we ran them on a distribution over only one of the possible factory games, the oracle algorithms would perform exactly the same, and the offline algorithms would be identical to oracle, as is always the case with distributions with supports of size 1.\filip{masirovat}

\subsection{Supermodular}

% remind what supermodular is
The class of supermodular functions/games is videly studied in many fields, espetially in cooperative game theory.
This leads us to investigating of what our Divergence, and Utopian Gap espetially, can tell us about the structure of this class.
We study a uniform distribution of supermodular games on $ n $ players, with the value of the grand coalition $ N $ set to $ 1 $\filip{masirovat}, which we call \filip{TODO}.
In our experiments, to generate a supermodular game uniformly at random, we have used the work of \filip{TODO}.
The results of those experiments are presented in \Cref{fig: exploitability over time}~(right).

% explain the observed
To our surprise, our algorithms work remarkably well on the \filip{TODO} distribution, vastly outperforming the random approach, and all achieving over 99~\% of Gap reduction with just the budget of five coalitions.
Also, interestingly, there is no difference between the algorithms themselves, suggesting that knowledge of the specific function drawn from the distribution does not help when decreasing the Gap.
We investigate these findings further in \Cref{ssec:largest}.

\subsection{$ L_k $ Norms}

% ell = l_1
So far, we have seen how our algorithms behave through the lens of cooperative games, with Utopian Gap as the Divergence.
In this chapter, we broaden the scope to general set functions.
Now, instead of being interested in the ambiguity of the Shapley value (which is what the Gap measures), we will use the more generally used $ L_k $-norms as the Divergence function $ \ell $.
The Divergence function and the intuition behind it was described in detail in \Cref{sec:divergence}, but as a reminder to the reader, the Divergence is an $ \ell $-norm of the difference between the upper and the lower functions $ \uf f $ and $ \lf f $.

% TODO: co to dělá
We have conducted the experiments with the $ L_1 $ and $ L_2 $ norms, as those are the most commonly used and with a clear and natural interpretation.\filip{tohle "interpretation" vysvětlit dál, nebo odcitovat?}
The results of those experiments on the factory and supermodular distributions are presented in \Cref{fig:l12}.
Perhaps surprisingly, the results are very similar to those with the Utopian Gap.
\filip{nevim co dál k tomuhle}

\subsection{Largest Coalition Heuristic}

\label{ssec:largest}
\filip{co dát tohle úplně na konec, ať je to jako happy end? :D}

% explain motivation
In the previous sections, we have seen that our methods work remarkably well on the uniform supermodular distribution for $ n=5 $.
Furthermore, all the algorithms end up choosing the same actions: revealing all the subsets\footnote{or coalitions, but I will stick to the more general set function terminology in this section} of size $ n-1 $.
After those are revealed, the Divergence decreases by about 99 \% (depending on the choice of $ \ell $).
This lead us to propose the heuristic we call the Largest Coalitions, which, for a budget of $ \tau = 2n + 2 $, reveals the subsets $ \k = \k_0 \cup \left\{ N \setminus \left\{ i \right\} \suchthat i \in N \right\} $.

% comment on performance
\Cref{fig:largest} shows the performance of this heuristic with the Utopian Gap, and the $ L_1 $-Divergence for increasing $ n $.
In order to make the results comparable, we normalize the results by the initial Divergence (Gap), i.e. when $ \k = \k_0 $.
Unfortunately, we cannot compare the heuristic to the performance of the optimal algorithms, as that is computationally expensive, so we instead compare it only with the Random strategy.

% TODO: explain why
Surprisingly, while the normalized Divergence increases for the Random strategy as $ n $ grows, for our Largest Coalitions heuristic it stays the same, or even decreases, as \Cref{fig:largest} shows.
To understand why that is the case, we have analyzed the average values of subsets in the supermodular distribution, and we have found that the values decrease exponentially with the size of the subset.
This explains the good performance of our heuristic, as choosing large subsets limits the upper superadditive bounds of the smaller subsets the most.\filip{Jak moc to tu chci vysvětlovat?}

\subsection{Offline vs Oracle}
% TODO: offline beats oracle?
% \david{Možná spíš offline vs oracle?}
% \filip{Uuuuh, faaajn, ale offline beats oracle mi přišlo chytlavější teda xD}
% \david{Jakože zní to cool, ale ten title pak naznačuje, že je to takhle vždycky}

% re-introduce what it's about
In the above sections, we have proven many statements about the relationship between the solutions of the \algFO{} and \algRO{} algorithms, along with the relationships of the optimal algorithms and their greedy counterparts.
The last relationship we have not mentioned yet is that of \algFG{} and \algRG{}.
One would expect that the resulting valuation of \algFG{} would be an upper bound on \algRG{}, as was the case with the optimal variants.
This has been the case in all the figures we have seen so far.
However, as we show in \Cref{fig:offlinebeatsoracle}, it is \emph{not} true in general.

% TODO: explain l-inf
Our example uses the $ L_\infty $ norm as $ \ell $, which is defined as \[
  \fce {L_\infty} x \deq \max_i x_i.
\]

% TODO: explain why

\filip{schůzka: nevím kolik a na co tam chceme dát ty obrázky přes celou stránku}
