\chapter*{Introduction}
\addcontentsline{toc}{chapter}{Introduction}

Set functions offer a way to express the relationship between subsets of some finite ground set.
This is used in countless fields, e.g., as utility of a player based on received items in combinatorial auctions, or for modeling the value of coalitions in cooperative game theory.
\david{ještě bych vymyslel další příklady}

However, in applying set functions to a real-world problem, one quickly reaches a significant roadblock: their size.
A set function assigns a value to every subset of a set $ N $.
The size of a set function, then, grows exponentially in the size of $ N $.
In many applications, obtaining even a single value of the set function is hard -- it costs, e.g., money, time, or computational resources.
% TODO: more memes: well everyone knows its scary, but how scary?
% If $ N $ contains more than 300 elements, the number of subsets, and thus the size of the set function, is larger than the number of particles in the universe.

% TODO: timhle odstavcem si nejsem jistej
% Throughout the literature, we can find many ways to circumvent this problem.
% For example, consider a set function $ f $ which is additive, meaning $ \fce{f}{S} = \sum_{i \in S}^{} \fce{f}{\left\{ i \right\}} $.
% Then the whole set function is precisely given by just the values $ \fce{f}{\left\{ i \right\}} $ for all $ i \in N $.
% While this solution is sufficient in many situations, it vastly limits the set functions we are allowed to work with.

In this thesis, we investigate a different approach.\filip{tady ted neni jasny z ceho "different", protoze se jeste rozhoduju jestli nad tohle nedam jeden odstaec o ostatnich pristupech}\david{jn souhlas, ten odstavec by to podle mě chtělo}
We assume to have a limit of how many subsets we are willing/able to reveal.\david{tahle věta zní divně. At maximum mi zní nic moc. Celkově bych řekl něco jakože uvažujeme, že dokážeme získat pouze omezený počet subsetů? Ten emphasis na budget mi evokuje, že je to definice toho slova. A jako jo, ale až na skoro konci bakalářky}\filip{lepší?}
We further assume the function comes from a given class of set functions, and we can thus bound the remaining values.\david{tohle je taky trochu random - nemělo by se tímhle začít?}\filip{nevim no...}
This introduces \emph{ambiguity} to our model.
Our aim is to choose which subsets to reveal, such that the resulting ambiguity in the values of the unknown subsets is minimized.
We frame this as an optimization problem, and explore multiple approaches for solving it.
First, we investigate exact search techniques, and later we turn to reinforcement learning algorithms for approximate solution.

\section*{Organization and Contributions}

We begin by introducing set functions, their basic properties and classification.
On top of the set functions, we build the theory of cooperative games.
Further, we offer an introduction to reinforcement learning, focusing mainly on one of the current state-of-the-art algorithms, called PPO.
We continue by defining incomplete set functions, formally introducing ambiguity to our setup.
To measure the ambiguity, we introduce the \emph{divergence}.\david{Hmm, není torchu divný první říct, "formally introducing ambiguity to our model" a až pak říct že jí měříme divergencí?}\filip{podle mě ne?}
Finally, we define two optimization problems to minimize the divergence, one in an online, and the other in an offline setting.

This setup was originally stated by\david{in?}\filip{podle mě ne, je to jakože "byl definován Úradníkem atd", nedává smysl "byl definován v Úradníkovi". nebo možná dává, ale zní to bolestivě} \cite{uradnik2024reducing}, focusing purely on the setting of cooperative game theory.
In this thesis, we extend these ideas to be applicable to general set functions.
We offer a more extensive theoretical analysis of the approaches proposed by \cite{uradnik2024reducing}, including comparison of the online and offline approaches, and a formal proof of the time complexity of all proposed algorithms.
The divergence is a generalization of their utopian gap, which has a motivation purely\david{purely je za mě navíc}\filip{za mě ne?} in cooperative game theory, to a more general valuation applicable to general set functions.
We analyze this generalization theoretically, and then offer an empirical evaluation and comparison of the valuations.

\section*{Related Work}

\filip{Hele tak asi jsem si ten related work vycucal z prstu, v ostatnich bakalarkach takhle neni. nekdo to tam nema vubec, nekdo to ma jako vlastni kapitolu... co s tim?}
\david{asi záleží jak moc chceš být tryhard :) Za mě můžeš určitě vykuchat články, vypadalo by to podle mě dobře. Ale jestli se ti do toho nechce, určitě to nikoho neurazí.}
